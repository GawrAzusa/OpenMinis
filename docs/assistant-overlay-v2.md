# Minis 系统小窗助理（v2）

这是基于 Android 正规数字助理接口的独立实现，不包含 Gemini 的品牌、素材或私有接口。普通启动图标仍打开原应用；系统助理入口使用 `VoiceInteractionService` / `VoiceInteractionSession`。

## 交互

- 墨蓝渐变描边小窗：文字输入、录音、截图、屏幕共享、对话展开、进入完整聊天、声音开关、新对话、停止/继续。
- 系统助理手势/电源键唤起默认进入原声聆听，无需再点语音；已存在的草稿或待发音频不被覆盖或自动发送。点击输入框或打字按钮立即取消待开始/进行中的录音，切换文字输入。
- 首次麦克风授权只续接当前小窗内的那一次请求；拒绝、Home、关闭、锁屏、换会话后旧请求失效。截图/工具/小球恢复和进程重建不会自行开麦。
- 点击 `−` 收成可拖动、贴边的小球。**挂起是暂停任务，不是让 Agent 在后台继续点击手机**。录音和播报一起停止；恢复小窗不会自动恢复操作，继续任务须明确点击。
- 系统限制悬浮窗的页面可隐藏小球；锁屏停止屏幕共享和录音，不绕过系统保护。
- 文字和手动附件通过发送按钮进入任务；明确唤起或点击语音后，检测到持续声音并结束这一段时会按独立语音输入配置处理，再经同一发送入口附加已选择的画面。单独进入设置或授权不是新发送授权。
- 工具权限请求按当前聊天归属显示；需要 Android 系统权限时可明确展开原聊天完成授权，不自动代替用户允许。

## 从旧版升级

保留同一包名和签名下的应用数据，不需要卸载。若已把旧版 Minis 设置为默认助理，部分 Android 系统仍保留旧 Activity 入口：请在系统默认数字助理页先选“无”，再选回 Minis。应用按实际激活的语音服务判断是否设置完成，不只检查包名。

## 语音的准确边界

- 小窗录制有大小/时长边界的 PCM WAV。有明确的云端 Voice Input 选择时，音频只送该听写服务，转写文字进入任务模型；UI 明示“语音转写”，**不把转写模式冒充主模型原声理解**。Voice Input、任务模型和 Voice Output 分别配置，切换任务模型不替换听写/朗读服务。
- Google 分离方案：`gemini-3.5-transcribe` 独立听写 → 用户已有 GPT OAuth 任务模型 → `gemini-3.8-flash-lite-tts` 朗读最终答复。专用听写请求不包含任务工具或聊天历史。具体账户权限/额度与真实服务可用性仍需实测。
- 无云端 Voice Input 选择时保留原声直传模式，不隐式调用系统 ASR。录音前固定输入路由，转写失败保留本次录音供同路由重试，不偷偷换模型；空白听写不发送。打字、暂停、关闭、Home 或换会话撤销转写，迟到结果不得覆盖草稿或发起任务。
- 实现 Gemini 原生 `inlineData` 音频及 OpenAI 兼容 Chat Completions `input_audio` 路径。模型必须按配置声明音频输入、工具调用能力；图片还需视觉能力。供应商/模型不兼容时明确拒绝，不换供应商。
- 这是按回合发送音频，**不是 Gemini Live 或双向实时音频会话**。本地能量端点在持续声音后约1.5秒停顿自动提交；约10秒没有持续声音则停止、不发送；每段最长60秒。仍可手动结束。
- 端点检测不转写或修改 WAV，但也不是语义语音识别：持续噪声/音乐可能误触发，轻声可能漏检；不能宣称与 Gemini 的语音体验一模一样。关闭、转打字、暂停会撤销待提交录音。
- 回复使用现有可配置的语音输出服务。小窗已选择云端自然语音时，合成/播放失败会明确显示不可用，**不会自动换回系统声音**。当前语音选择为系统声音时才走该路径；没有把系统 TTS 重新命名为“自然语音”。普通聊天的兼容回退策略不受此限制改动影响。
- 当前 OpenAI OAuth 接入走 Codex/Responses，可用于文字、看图和工具任务，**不属于本实现的原声音频输入通道**；可以接收明确启用的独立听写服务输出的文字。OAuth 登录成功不代表已配置 Google 听写或自然语音输出服务，也不包含其费用。
- 语音发起的有效任务默认开启回复播报；TTS 仅接收最终回复文本。新 Gemini TTS 支持默认 WAV，旧型号的 PCM 输出保持兼容；风格/语速指令与朗读正文分离，不把方向说明当正文念出。
- 工具名、参数、命令和结果不再主动送到回复播报。小窗只选择新完成的最终助手回答；失败/部分输出不当作成功回复播报，并沿用文本清理。
- 自然音色质量、真实音频模型理解和工具执行，需要实际配置的服务与真实试听/任务验收；本地协议测试不能代替这些证据。

## 截图与共享

- 先取得通知权限，便于明确停止，再通过系统 MediaProjection 选择单应用或整个屏幕。拒绝后仍可使用文字功能。
- 一次截图自动结束投屏，留下本地待发送预览；移除/关闭/锁屏会清除。
- 连续共享只保留有界的最新帧以及用户本次选择的预览帧，不保存录像。每次点击发送时隐藏自身小窗，取更新后的画面；**不是持续把每一帧传给云端模型**。
- 停止、撤销、锁屏或不可见/受保护画面会使预览与文件失效。已明确发送并进入聊天历史的附件属于聊天记录，而不是临时共享缓存。
- 权限弹窗返回、延迟截图和共享发送都有当前操作归属检查；Home、关闭、换会话后不能用过期回调偷偷发起任务或重开小窗。

## 数据与取消

沿用原 ChatViewModel、持久化和手机工具，不创建另一个假聊天后端。保留发送中的文字/音频，直到本地数据库与聊天历史明确接收；取消发生在写入前则保留待发内容，已写入则继续原任务而非重复提交。仅本地消息事务使用短暂不可取消区间，模型网络和手机操作没有被屏蔽取消。

## 验证与分发

- 只使用聚焦的 assistant instrumentation 源集；上游全套设备测试的旧接口问题没有通过删除测试来伪造通过。
- 新完成的任务在关闭/挂起时不再被重标记为中断任务；真正执行中的任务仍可暂停并明确继续。纯音频历史保留可见消息和原始载荷，重试以原数据库消息 ID 定位，不能按可见列表位置误选旧指令。
- 透明小窗会先清除自己的绘制区域，再画卡片，避免键盘移动后保留旧帧；不靠设置截图保护标志来隐藏视觉问题。
- NDK 明确选择 r28，修复本项目三个 JNI 库的段对齐并保留所有随包库。`deps/check_android_native_alignment.py` 独立检查 ELF 和 ZIP 对齐；未改动的 32 位兼容 loader 仍有 4KB 段，**不宣称所有架构已完整通过 16KB 验收**。
- 设备 instrumentation 或强行停止应用可能留下“设置仍选 Minis，但主语音服务未连接”的状态。验证前须确认主 `VoiceInteractionService` 已真正绑定；必要时在系统默认助理页重新选择。不能仅凭 `KEYCODE_ASSIST` 能显示会话窗口，就认定主服务已正常工作。
- 内部 debug APK 与上游正式签名可能不同；安装冲突时不要先卸载以免丢失数据。
- 精确源码、APK 哈希、真机/模拟器证据与未验收项由本次候选清单和验收报告记录。构建完成不等于真实模型端到端通过；源码 Draft PR 不等于公开二进制发布。
